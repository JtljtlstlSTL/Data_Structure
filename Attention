1.顺序存储结构中数据元素之间逻辑关系由存储位置表示；
2.有序表是与逻辑结构有关的术语；
3.对算法的评价不包括并行性；
4.算法的高效性有空间时间两方面；

5.存储密度：数据元素存储量/（数据元素存储量+指针域存储量）；

6.普通队列元素个数：尾指针与头指针差值
  循环队列差值可能为负，要加个n;

7.树转化成二叉树的方法
  1>树中所有相邻兄弟节点之间加一条线；
  2>对树中每个结点只保留他与长子之间的连线，删除与其他孩子之间的连线；
8.森林转化为二叉树的步骤
  1>将森林中的每棵树转化成相应的二叉树；
  2>第一颗二叉树不动，从第二颗树开始，依次把后一棵二叉树的根结点作为前一棵二叉树根结点的右孩子结点，
    直到所有二叉树都连接到一起；
  PS.n棵树时，边数=节点数-n
9.有n个元素入栈，以f（n）表示出栈种数
  f（0）=0，f（1）=1，f（n）=f（k-1）*f（n-k）（k=1~n）
